# A Port Scanner 
A lightweight network scanner written in C++ that detects active hosts and open ports on a network.

## Phase 1
Create a main.cpp file to scan for open ports that are listening on a target host.

## Phase 2
Host Discovery added: Scans if host is alive before scanning for open ports listening"
